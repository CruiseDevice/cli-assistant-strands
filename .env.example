# ==========================================
# CLI Assistant Environment Configuration
# ==========================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your values (never commit .env to Git!)
# 3. Keep .env in your .gitignore
#
# SECURITY: Never commit this file with real values!
# ==========================================

# ============ AWS CONFIGURATION ============
# AWS region for Bedrock (us-west-2 recommended for model availability)
AWS_REGION=us-west-2

# AWS Profile name (from ~/.aws/credentials)
# Leave empty to use default profile
# Use named profile for better security: AWS_PROFILE=strands-cli
AWS_PROFILE=strands-cli

# Optional: Explicit credentials (NOT RECOMMENDED - use profiles instead)
# Only use for CI/CD or containerized environments
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# ============ MODEL CONFIGURATION ============
# Default model to use (haiku, sonnet, opus)
# haiku = cheapest, fastest (testing)
# sonnet = balanced (production)
# opus = most capable (complex tasks)
DEFAULT_MODEL=haiku

# Streaming enabled by default
STREAMING_ENABLED=true

# ============ COST MANAGEMENT ============
# Daily budget limit in USD
# Stops execution when exceeded (if STOP_ON_BUDGET_EXCEEDED=true)
DAILY_BUDGET_LIMIT=1.00

# Monthly budget limit in USD
MONTHLY_BUDGET_LIMIT=10.00

# Auto-stop when budget exceeded (true/false)
STOP_ON_BUDGET_EXCEEDED=true

# Cost alert thresholds (comma-separated percentages)
COST_ALERT_THRESHOLDS=0.5,0.8,0.95

# ============ LOGGING CONFIGURATION ============
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log directory
LOG_DIR=logs

# Enable console logging (in addition to file logging)
CONSOLE_LOGGING=false

# Enable performance tracking
TRACK_PERFORMANCE=true

# ============ SESSION CONFIGURATION ============
# Directory for session storage
SESSION_DIR=sessions

# Maximum context tokens to send to model (cost optimization)
# Lower = cheaper but less context
# Recommended: 4000 for balance, 8000 for more context
MAX_CONTEXT_TOKENS=4000

# Maximum messages in context (cost optimization)
MAX_MESSAGES_IN_CONTEXT=20

# Auto-save sessions
AUTO_SAVE_SESSIONS=true

# Session retention in days (cleanup old sessions)
SESSION_RETENTION_DAYS=30

# ============ TOOL CONFIGURATION ============
# Enable/disable specific tools
ENABLE_WEB_SEARCH=true
ENABLE_PYTHON_REPL=true
ENABLE_SYSTEM_INFO=true

# Web search settings
WEB_SEARCH_MAX_RESULTS=3
WEB_SEARCH_TIMEOUT=5

# Python REPL timeout (seconds)
PYTHON_REPL_TIMEOUT=10

# ============ SECURITY SETTINGS ============
# Input validation
ENABLE_INPUT_VALIDATION=true

# Maximum input length (characters)
MAX_INPUT_LENGTH=10000

# File operation restrictions
ALLOWED_FILE_EXTENSIONS=.txt,.md,.json,.csv,.py
MAX_FILE_SIZE_MB=5

# Rate limiting (requests per minute)
RATE_LIMIT=60

# ============ UI SETTINGS ============
# Enable colored output
COLOR_OUTPUT=true

# Show cost warnings
SHOW_COST_WARNINGS=true

# Show tool usage indicators
SHOW_TOOL_USAGE=true

# Show loading indicators
SHOW_LOADING=true

# Command history size
HISTORY_SIZE=100

# ============ ADVANCED SETTINGS ============
# Environment (development, staging, production)
ENVIRONMENT=development

# Debug mode (more verbose logging)
DEBUG=false

# Bedrock request timeout (seconds)
BEDROCK_TIMEOUT=30

# Bedrock retry attempts
BEDROCK_RETRY_ATTEMPTS=3

# Bedrock retry delay (seconds)
BEDROCK_RETRY_DELAY=1.0

# ============ OPTIONAL: ANTHROPIC API ============
# Only needed if using Anthropic API directly instead of Bedrock
# Leave empty if using Bedrock
# ANTHROPIC_API_KEY=sk-ant-...

# ============ OPTIONAL: CLOUDWATCH ============
# CloudWatch log group (optional, has cost)
# CLOUDWATCH_LOG_GROUP=/aws/strands/cli-assistant
# CLOUDWATCH_LOG_STREAM=cli-assistant

# ============ OPTIONAL: SECRETS MANAGER ============
# Use AWS Secrets Manager for production (optional, has cost)
# USE_SECRETS_MANAGER=false
# SECRETS_MANAGER_SECRET_NAME=cli-assistant/prod

# ============ DEVELOPMENT ONLY ============
# Mock Bedrock responses for testing (no API calls)
# MOCK_BEDROCK=false

# Disable cost tracking for testing
# DISABLE_COST_TRACKING=false

# ==========================================
# END OF CONFIGURATION
# ==========================================

# NOTES:
# - Values are case-sensitive
# - Boolean values: true/false (lowercase)
# - Paths: Use forward slashes (/) or system-appropriate separators
# - Comments: Lines starting with # are ignored
#
# SECURITY REMINDERS:
# ✅ Never commit .env to Git
# ✅ Use AWS profiles instead of hardcoded credentials
# ✅ Rotate credentials every 90 days
# ✅ Enable MFA on AWS account
# ✅ Use separate profiles for dev/prod
# ✅ Review and update this file regularly
